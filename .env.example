# Copy to .env.local (and set the same two vars in Vercel → Project → Settings → Environment Variables).
# Both come from the same Supabase project the mobile app uses.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
